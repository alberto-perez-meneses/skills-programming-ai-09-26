/**
 * @file Contrato de datos de un usuario persistido.
 * @module src/interfaces/user
 */

/**
 * Usuario de la tabla `users`.
 *
 * @typedef {Object} User
 * @property {number} id Identificador numérico autoincremental.
 * @property {string} name Nombre visible.
 * @property {string|null} email Correo único; puede ser nulo.
 * @property {Date} createdAt Fecha de creación (`created_at`).
 * @property {Date} updatedAt Fecha de última actualización (`updated_at`).
 */

module.exports = {};
